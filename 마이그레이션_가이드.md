# 주문필요산출 → socp-erp 마이그레이션 가이드

> 이 가이드는 독립 앱 `C:/project/order/`의 **주문필요산출** 기능을
> `C:/project/socp-erp/` 프로젝트의 메뉴 항목으로 통합하기 위한 **완전한 지시사항**입니다.
> 새 Claude Code 세션에서 socp-erp 프로젝트를 열고 이 가이드를 따르세요.

---

## 1. 사전 조건

- socp-erp는 **React 18 + Vite + react-router-dom v7 + Tailwind CSS** 프로젝트
- `xlsx` 패키지가 이미 `package.json`에 존재 (추가 설치 불필요)
- socp-erp 기능 페이지 패턴: `src/features/<FeatureName>/<FeatureName>Page.jsx`
- 메뉴 등록: `src/App.jsx`의 `menuItems` 배열 + `src/main.jsx`의 `<Route>`

---

## 2. 폴더 구조 생성

```
src/features/OrderCalculation/
├── OrderCalculationPage.jsx    ← 메인 페이지 (기존 App.jsx 통합)
├── components/
│   ├── DataInputSection.jsx    ← 기존 DataInputPage.jsx 적응
│   ├── ResultTable.jsx         ← 기존 ResultTable.jsx (거의 동일)
│   ├── SummaryCards.jsx        ← 기존 SummaryCards.jsx (거의 동일)
│   ├── SettingsSection.jsx     ← 기존 SettingsPage.jsx 적응
│   ├── FileUploader.jsx        ← 기존 FileUploader.jsx (동일)
│   └── PasteInput.jsx          ← 기존 PasteInput.jsx (동일)
└── utils/
    ├── constants.js            ← 기존 constants.js (동일)
    ├── calculator.js           ← 기존 calculator.js (동일)
    └── masterData.js           ← 기존 masterData.js (localStorage 키 변경)
```

---

## 3. 파일별 생성/복사 지침

### 3-1. `utils/constants.js` — 그대로 복사

원본: `C:/project/order/src/utils/constants.js`

```javascript
// ─── Shared Constants ────────────────────────────────────────────────────────

export const ASSIGNEES = ['정호', '희철', '해준', '길남'];

export const ASSIGNEE_COLORS = {
  '정호': { dot: 'bg-blue-500', text: 'text-blue-700', badge: 'bg-blue-100 text-blue-800' },
  '희철': { dot: 'bg-green-500', text: 'text-green-700', badge: 'bg-green-100 text-green-800' },
  '해준': { dot: 'bg-purple-500', text: 'text-purple-700', badge: 'bg-purple-100 text-purple-800' },
  '길남': { dot: 'bg-orange-500', text: 'text-orange-700', badge: 'bg-orange-100 text-orange-800' },
};
```

### 3-2. `utils/calculator.js` — 그대로 복사

원본: `C:/project/order/src/utils/calculator.js`
변경 없이 전체 파일을 복사합니다.

### 3-3. `utils/masterData.js` — localStorage 키 프리픽스 변경

원본: `C:/project/order/src/utils/masterData.js`

**중요 변경사항**: socp-erp의 다른 기능과 localStorage 충돌을 방지하기 위해 키 프리픽스를 추가합니다.

변경할 부분:
```javascript
// 변경 전
const STORAGE_KEYS = {
  skuVendor: 'master_sku_vendor',
  assignment: 'master_assignment',
  monthlyVendor: 'master_monthly_vendor',
};

// 변경 후
const STORAGE_KEYS = {
  skuVendor: 'oc_master_sku_vendor',
  assignment: 'oc_master_assignment',
  monthlyVendor: 'oc_master_monthly_vendor',
};
```

그리고 settings 키도 변경:
```javascript
// 변경 전
localStorage.getItem('app_settings')
localStorage.setItem('app_settings', ...)

// 변경 후
localStorage.getItem('oc_app_settings')
localStorage.setItem('oc_app_settings', ...)
```

나머지 코드는 동일하게 복사합니다.

### 3-4. `components/FileUploader.jsx` — 그대로 복사

원본: `C:/project/order/src/components/FileUploader.jsx`
변경 없이 전체 파일을 복사합니다.

### 3-5. `components/PasteInput.jsx` — 그대로 복사

원본: `C:/project/order/src/components/PasteInput.jsx`
변경 없이 전체 파일을 복사합니다.

### 3-6. `components/SummaryCards.jsx` — import 경로만 변경

원본: `C:/project/order/src/components/SummaryCards.jsx`

import 경로를 변경:
```javascript
// 변경 전
import { ASSIGNEES, ASSIGNEE_COLORS } from '../utils/constants';

// 변경 후 (동일 — 상대경로가 같으므로 그대로)
import { ASSIGNEES, ASSIGNEE_COLORS } from '../utils/constants';
```

나머지 코드는 동일하게 복사합니다.

### 3-7. `components/ResultTable.jsx` — import 경로만 변경

원본: `C:/project/order/src/components/ResultTable.jsx`

import 경로 변경:
```javascript
// 변경 전
import { ASSIGNEES, ASSIGNEE_COLORS } from '../utils/constants';

// 변경 후 (동일 — 상대경로가 같으므로 그대로)
import { ASSIGNEES, ASSIGNEE_COLORS } from '../utils/constants';
```

나머지 코드는 동일하게 복사합니다.

### 3-8. `components/SettingsSection.jsx` — import 경로만 변경

원본: `C:/project/order/src/components/SettingsPage.jsx`
파일명을 `SettingsSection.jsx`로 변경합니다.

import 경로 변경:
```javascript
// 변경 전
import { getMasterData, importFromXlsx, clearMasterData, hasMasterData, getSettings, saveSettings } from '../utils/masterData';
import { ASSIGNEES } from '../utils/constants';

// 변경 후 (동일 — 상대경로가 같으므로 그대로)
import { getMasterData, importFromXlsx, clearMasterData, hasMasterData, getSettings, saveSettings } from '../utils/masterData';
import { ASSIGNEES } from '../utils/constants';
```

**export 이름 변경:**
```javascript
// 변경 전
export default function SettingsPage() {

// 변경 후
export default function SettingsSection() {
```

나머지 코드는 동일합니다.

### 3-9. `components/DataInputSection.jsx` — 기존 DataInputPage.jsx 적응

원본: `C:/project/order/src/components/DataInputPage.jsx`

변경 사항:
1. 파일명/함수명 변경: `DataInputPage` → `DataInputSection`
2. import 경로 변경 (같은 디렉토리 내 컴포넌트)

```javascript
import { useState } from 'react';
import FileUploader from './FileUploader';
import PasteInput from './PasteInput';
import {
  parseOrderFile,
  parseInventoryFile,
  parseTransitFile,
  parseTransitFromText,
  parsePrevConfirmedFile,
  calculate,
  autoAssignUnassigned,
} from '../utils/calculator';
import { getMasterDataAsMap, hasMasterData, getSettings } from '../utils/masterData';
import { ASSIGNEES } from '../utils/constants';

export default function DataInputSection({ onCalculate, adjustmentRate }) {
  // ... 나머지 코드는 원본 DataInputPage.jsx와 완전히 동일
}

function SectionLabel({ num, title, required, optional }) {
  // ... 원본과 동일
}
```

### 3-10. `OrderCalculationPage.jsx` — 메인 페이지 (핵심 변경)

기존 `C:/project/order/src/App.jsx`를 기반으로 하되 socp-erp 패턴에 맞게 적응:

```javascript
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import DataInputSection from './components/DataInputSection';
import SettingsSection from './components/SettingsSection';
import ResultTable from './components/ResultTable';
import SummaryCards from './components/SummaryCards';
import { hasMasterData } from './utils/masterData';

const TABS = [
  { id: 'input',    label: '데이터 입력' },
  { id: 'results',  label: '산출 결과' },
  { id: 'settings', label: '설정' },
];

export default function OrderCalculationPage() {
  const navigate = useNavigate();
  const [activeTab, setActiveTab] = useState('input');
  const [results, setResults] = useState(null);
  const [adjustmentRate, setAdjustmentRate] = useState(1.0);

  const masterStatus = hasMasterData();
  const masterMissing = !masterStatus.skuVendor || !masterStatus.assignment;

  const handleCalculate = (res) => {
    setResults(res);
    setActiveTab('results');
  };

  return (
    <div className="min-h-screen bg-slate-100">

      {/* ── Header ── */}
      <header className="bg-slate-800 text-white shadow-lg">
        <div className="max-w-7xl mx-auto px-6 py-0">
          <div className="flex items-center justify-between h-14">
            {/* Back + Title */}
            <div className="flex items-center gap-3">
              <button
                onClick={() => navigate('/')}
                className="px-3 py-1.5 text-sm text-slate-300 hover:text-white hover:bg-slate-700 rounded-lg transition cursor-pointer"
              >
                ← 메뉴로
              </button>
              <div className="w-px h-6 bg-slate-600" />
              <div className="w-7 h-7 bg-blue-500 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg className="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2}
                    d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                </svg>
              </div>
              <span className="font-bold text-base tracking-tight">주문필요산출</span>
            </div>

            {/* Tabs */}
            <nav className="flex items-center gap-1">
              {TABS.map(tab => {
                const isActive = activeTab === tab.id;
                const showBadge = tab.id === 'settings' && masterMissing;
                return (
                  <button
                    key={tab.id}
                    onClick={() => setActiveTab(tab.id)}
                    className={`relative px-4 py-1.5 rounded-full text-sm font-semibold transition-all cursor-pointer
                      ${isActive
                        ? 'bg-blue-600 text-white shadow-md'
                        : 'text-slate-300 hover:text-white hover:bg-slate-700'
                      }`}
                  >
                    {tab.label}
                    {showBadge && (
                      <span className="absolute -top-0.5 -right-0.5 w-2 h-2 bg-amber-400 rounded-full" />
                    )}
                  </button>
                );
              })}
            </nav>
          </div>
        </div>
      </header>

      {/* ── Content ── */}
      <main className="max-w-7xl mx-auto px-6 py-6">
        {activeTab === 'input' && (
          <DataInputSection
            onCalculate={handleCalculate}
            adjustmentRate={adjustmentRate}
          />
        )}

        {activeTab === 'results' && (
          <>
            {results ? (
              <>
                <div className="bg-white rounded-2xl shadow-sm border border-slate-200 px-6 py-4 mb-5">
                  <div className="flex items-center gap-4 flex-wrap">
                    <span className="font-semibold text-slate-800 text-sm whitespace-nowrap">조정 배율</span>
                    <input
                      type="range"
                      min="0.5"
                      max="2.0"
                      step="0.05"
                      value={adjustmentRate}
                      onChange={e => setAdjustmentRate(Number(e.target.value))}
                      className="flex-1 min-w-32 max-w-72 accent-blue-600"
                    />
                    <span className="text-sm font-bold text-blue-600 w-12 text-right tabular-nums">
                      ×{adjustmentRate.toFixed(2)}
                    </span>
                    <span className="text-xs text-slate-400">
                      (발주수량에 배율을 곱해 주문필요수량 산출)
                    </span>
                    <button
                      onClick={() => setActiveTab('input')}
                      className="ml-auto text-sm text-blue-600 hover:text-blue-800 font-semibold
                        underline underline-offset-2 cursor-pointer transition"
                    >
                      재산출 →
                    </button>
                  </div>
                </div>

                <SummaryCards summary={results.summary} />
                <div className="mt-5">
                  <ResultTable rows={results.rows} summary={results.summary} />
                </div>
              </>
            ) : (
              <EmptyResults onGoToInput={() => setActiveTab('input')} />
            )}
          </>
        )}

        {activeTab === 'settings' && <SettingsSection />}
      </main>
    </div>
  );
}

function EmptyResults({ onGoToInput }) {
  return (
    <div className="flex flex-col items-center justify-center py-24 text-slate-400">
      <svg className="w-16 h-16 mb-4 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5}
          d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
      </svg>
      <p className="text-base font-semibold text-slate-500 mb-2">산출 결과가 없습니다</p>
      <p className="text-sm mb-6">데이터 입력 탭에서 파일을 업로드하고 산출을 실행하세요.</p>
      <button
        onClick={onGoToInput}
        className="px-6 py-2.5 bg-blue-600 hover:bg-blue-700 text-white text-sm font-semibold
          rounded-xl transition cursor-pointer shadow-md shadow-blue-200"
      >
        데이터 입력으로 이동
      </button>
    </div>
  );
}
```

---

## 4. socp-erp 기존 파일 수정

### 4-1. `src/App.jsx` — menuItems 배열에 항목 추가

`menuItems` 배열의 마지막 항목(master-data) 뒤에 다음을 추가:

```javascript
        {
            id: 'order-calculation',
            path: '/order-calculation',
            title: '주문산출',
            color: 'blue',
            icon: (
                <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                    <path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"></path>
                    <path d="M9 5a2 2 0 0 1 2-2h2a2 2 0 0 1 2 2"></path>
                    <path d="M9 14l2 2 4-4"></path>
                </svg>
            ),
        },
```

> **참고**: `color: 'blue'`는 이미 사용중이므로 다른 색을 원하면 변경 가능.
> 사용 가능한 색상: purple, green, blue, orange, red, indigo, cyan, rose, teal

### 4-2. `src/main.jsx` — Route 추가

**import 추가** (다른 feature import 근처에):
```javascript
import OrderCalculationPage from './features/OrderCalculation/OrderCalculationPage.jsx'
```

**Route 추가** (`<Routes>` 블록 안에):
```javascript
<Route path="/order-calculation" element={<OrderCalculationPage />} />
```

---

## 5. 요약 체크리스트

| # | 작업 | 파일 |
|---|------|------|
| 1 | 폴더 생성 | `src/features/OrderCalculation/`, `components/`, `utils/` |
| 2 | utils 복사 | `constants.js` (동일), `calculator.js` (동일), `masterData.js` (키 프리픽스 `oc_`) |
| 3 | 컴포넌트 복사 | `FileUploader.jsx` (동일), `PasteInput.jsx` (동일) |
| 4 | 컴포넌트 적응 | `SummaryCards.jsx`, `ResultTable.jsx` (import 경로만), `SettingsSection.jsx` (이름변경), `DataInputSection.jsx` (이름변경) |
| 5 | 메인 페이지 생성 | `OrderCalculationPage.jsx` (← 메뉴로 버튼 + useNavigate) |
| 6 | App.jsx 수정 | menuItems에 order-calculation 항목 추가 |
| 7 | main.jsx 수정 | import + Route 추가 |
| 8 | 빌드 확인 | `npm run build` 성공 확인 |

---

## 6. 원본 파일 위치 참조

| 대상 | 원본 경로 |
|------|----------|
| App.jsx (메인 페이지) | `C:/project/order/src/App.jsx` |
| DataInputPage.jsx | `C:/project/order/src/components/DataInputPage.jsx` |
| ResultTable.jsx | `C:/project/order/src/components/ResultTable.jsx` |
| SummaryCards.jsx | `C:/project/order/src/components/SummaryCards.jsx` |
| SettingsPage.jsx | `C:/project/order/src/components/SettingsPage.jsx` |
| FileUploader.jsx | `C:/project/order/src/components/FileUploader.jsx` |
| PasteInput.jsx | `C:/project/order/src/components/PasteInput.jsx` |
| constants.js | `C:/project/order/src/utils/constants.js` |
| calculator.js | `C:/project/order/src/utils/calculator.js` |
| masterData.js | `C:/project/order/src/utils/masterData.js` |

> **작업 방법**: 각 원본 파일을 Read로 읽고, 이 가이드의 변경 사항을 적용하여 대상 경로에 Write합니다.
> 변경 없이 복사할 파일은 Read 후 그대로 Write하면 됩니다.

---

## 7. 핵심 차이점 요약

| 항목 | 독립 앱 (order) | socp-erp 통합 |
|------|----------------|---------------|
| 라우팅 | 없음 (SPA) | react-router-dom, `← 메뉴로` 버튼 |
| localStorage 키 | `master_*`, `app_settings` | `oc_master_*`, `oc_app_settings` |
| 컴포넌트 이름 | DataInputPage, SettingsPage | DataInputSection, SettingsSection |
| 헤더 | 독립 헤더 | `← 메뉴로` 버튼이 포함된 헤더 |
| 파일 구조 | `src/components/`, `src/utils/` | `src/features/OrderCalculation/` 하위 |
